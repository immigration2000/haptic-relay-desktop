# Haptic Relay Desktop ?? ???

? ??? ???? ??? ???? ?, ??? ??, ???? ????, ?? ?? ??? ?????.

## 1. ?? ??

Haptic Relay Desktop? ?? ???? ???? ?? ??? ?? ???? ?? ?? ??? ??? ??? ??????.

??? ????? ??TV ?? ?? ?? ????? ??????, ?? ?? ?? ??? ????? ???? ???? ? ?? ?? ????.

```text
?? ?? ??? = ??, ??, ? ??
Haptic Relay ? = ? ??, ???? ??, ?? ??
Haptic Relay ?? = ? ??, ?? ??, ?? fanout
```

??? ? ???? ?? ??? ? ?? ??? ??? ????? ?????.

- ??? ??: ?? ???? ????? ???
- ? ??: ?? ??? ???, ? ??, ???, ?? ??? ??

## 2. ?? ??? ??

```text
1. ????? ?? ?? ????? ?? ??
2. ????? Haptic Relay Desktop ??
3. ??? ??? ?? URL ??
4. ? ??, ????, ?? ?? ??
5. Control API? ? ?? ? host token ??
6. ?? ??? relay node? WebSocket ??
7. ????? ? ??/????? ??? ??
8. ???? ? ?? ? ? ??
9. Control API? viewer token ??
10. ???? ??? viewer socket? ?? ?? ??? ?
11. ???? ??? ?? ??? ????? ??
12. ??? viewer ?? relay node room? ??
13. ???? ???? ??? 4??? packet?? relay
14. viewer ?? packet? decode
15. viewer ?? ?? motion frame? ??? ????? T-Code? ??
```

## 3. ?? ????

```text
src/App.tsx
  ???? ? UI. ????/??? ??, ? ??, ? ??, ???? ??, ??? ??.

electron/main.ts
  Electron main process. Renderer? native ?? ?? IPC ??.

electron/services/relay-client.ts
  Control API ??, Socket.IO relay ??, motion packet ??/??, ???? ?? ??? ??.

electron/services/hardware-controller.ts
  SerialPort ??, T-Code D1/D2 capability probe, ???? ?? queue, backpressure ??.

electron/services/tcode-encoder.ts
  ???? motion frame? OSR/SR6 ?? T-Code? ???? probe ??? ??.

server/src/relay-server.ts
  Control API, Socket.IO relay, motion fanout, metrics, healthcheck.

server/src/control-token.ts
  HMAC signed host/viewer token ?? ? ??.

server/src/room-registry.ts
  ? metadata ??, relay node ??, memory/Redis registry ??.

src/shared/motion-packet.ts
  ????? 4??? binary motion packet encode/decode.

scripts/relay-load-test.mjs
  500?/1000? ??? fanout ?? ???.
```

## 4. ?? ??

??? ?? ??? ?? Control API? Relay Node? ?? Node ???? ?? ????. ??? ?? ??? ??? ??? ? ?? ?????.

```text
Desktop App
  |
  | POST /api/rooms
  v
Control API
  - room metadata ??
  - relay node ??
  - host token ??
  |
  | hostToken + relayUrl
  v
Relay Node
  - WebSocket only
  - signed token ??
  - motion packet fanout
```

????? ?? ??? ???? ?? ?????.

```text
Control API
  - auth
  - room create/join
  - password / approval
  - relay node assignment
  - room registry persistence

Relay Nodes
  - WebSocket relay
  - room-local fanout
  - hot path metrics
  - no DB read per motion frame

Redis
  - room metadata
  - relay node assignment
  - short TTL room state

Postgres, later
  - user accounts
  - billing
  - moderation
  - audit logs
```

## 5. Control API

### `POST /api/rooms`

?? ???? host token? ?????.

??:

```json
{
  "roomName": "studio-main",
  "password": "1234",
  "entryMode": "open"
}
```

??:

```json
{
  "ok": true,
  "roomName": "studio-main",
  "entryMode": "open",
  "relayNodeId": "local-1",
  "relayUrl": "http://localhost:4174",
  "hostToken": "..."
}
```

### `POST /api/rooms/:roomName/join`

??? ??? ???? viewer token? ?????.

??:

```json
{
  "displayName": "viewer-01",
  "password": "1234"
}
```

??:

```json
{
  "ok": true,
  "roomName": "studio-main",
  "relayNodeId": "local-1",
  "relayUrl": "http://localhost:4174",
  "viewerToken": "..."
}
```

### `GET /healthz`

?? ?? ??????.

### `GET /metrics`

?? ?? ?, forwarded frame, dropped frame, relay node ??? ?????.
???? ?? `pendingApprovals`? ?? ?? viewer ?? ??? ? ????.
?? ?? ?? `blockedViewers`? ??? ? ????.

## 6. Relay Socket ??

Relay socket? token ?? ? ?? ?? ??? ???? ????.

```text
Host:
  Control API?? hostToken ??
  Socket.IO connect
  room:create { token: hostToken }
  reconnect ? ?? hostToken?? room:create ???
  motion packet emit: "m"
  ?? ?? ? room:stop ??

Viewer:
  Control API?? viewerToken ??
  Socket.IO connect
  viewer:join { token: viewerToken }
  reconnect ? ?? viewerToken?? viewer:join ???
  request mode? viewer:approval-requested? host ??? ???
  host? viewer:approve { socketId, approved } ??
  ??? viewer? room join ??
  ?? ??? ?? ??? ?? ? ?? ??? ??? ? ???? ???? ??
  host? viewer:moderate { socketId, action } ?? ??
  kick? ?? room?? ??, block? ?? ? ?? ?? ??? ?? ? ???? ??
  room:stop ?? ? motion queue ?? ? ????? 0 T-Code ??
  motion packet receive: "m"
  packet decode
  HardwareController.queueMotion()
  T-Code serial output
```

?? ??? ??? ??? ??? ?? `"m"`? ?????.

## 7. Token ??

`server/src/control-token.ts`? HMAC-SHA256 signed token? ?????.

Payload:

```json
{
  "role": "host",
  "roomName": "studio-main",
  "exp": 1784630400000
}
```

viewer token? `displayName`? ??? ? ????.

????? `HAPTIC_CONTROL_TOKEN_SECRET`? ??? ? ?? ??? ???? ???.

```text
HAPTIC_CONTROL_TOKEN_SECRET=long-random-secret
```

## 8. Room Registry

`server/src/room-registry.ts`? ? ?? registry driver? ?????.

### Memory Registry

??????.

```text
HAPTIC_ROOM_REGISTRY_DRIVER=memory
```

??:

- ?? ??? ???
- Redis ?? ?? ?? ??
- ?? ???? ??

??:

- ?? ????? ??? ? metadata? ???
- ?? Control API/Relay Node ?? ?? ??

### Redis Registry

?? ??????.

```text
HAPTIC_ROOM_REGISTRY_DRIVER=redis
HAPTIC_REDIS_URL=redis://localhost:6379
HAPTIC_ROOM_TTL_SECONDS=28800
```

Redis?? room metadata? relay node assignment? ?????.

??? ??:

```text
Redis? motion frame ??? ?? ???.
```

??? motion fanout? relay node? `activeRooms` cache?? ?????. Redis? ? ??? ?? ?? 500? ???? ??? ??? ?? ????.

## 9. Relay Node Assignment

`RelayDirectory`? relay node ??? ????, ? ?? ? node? ?????.

?? node ??:

```text
HAPTIC_RELAY_NODE_ID=local-1
HAPTIC_PUBLIC_RELAY_URL=http://localhost:4174
```

?? node ??:

```text
HAPTIC_RELAY_NODES=[
  {"id":"relay-seoul-1","url":"https://relay-seoul-1.example.com","maxViewers":500},
  {"id":"relay-seoul-2","url":"https://relay-seoul-2.example.com","maxViewers":500}
]
```

?? ?? ??? least-room-count???. ? ??? ?? ?? relay node? ? ?? ?????.

?? ??:

- node? ?? ?? ?
- CPU/event loop lag
- ??, ?: Seoul/Tokyo
- room ?? ??
- streamer ??

## 10. ???? ?? ????

?? ?? ?? motion payload? JSON? ????.

4??? binary packet???.

```text
byte 0-1: position uint16, big-endian, 0-65535
byte 2-3: intensity uint16, big-endian, 0-65535
```

?:

```text
position = 0.42
intensity = 0.8

packet = [107, 133, 204, 204]
```

??? ? ??:

- JSON field name ??
- timestamp ??
- roomName ??
- float ??? ??
- viewer ??? ???? outbound traffic ??

?? JSON ??? ? ??? ??~100????? ? ? ????. ?? packet payload? 4??????.

## 11. ???? ?? ????

???? packet? ?? ???? ??? ????.

Relay protocol:

```text
4-byte binary packet
```

Hardware protocol:

```text
T-Code ASCII over SerialPort
```

?? ?? ?? `D1`/`D2`? ?? ??? T-Code ??? ?? axis? best-effort? ?????. OSR/SR6 ???? ?? ??? ?? ? ???? probe ??? ??? ??? ????, UI?? ?? ?? ??? ?????.

?? ??? ?? motion queue?? ?????. ?? pending frame? ???? `DSTOP`? ?? ? ? 0 ??/0 ?? fallback T-Code? ???.

?? ??:

```text
D1
D2
L04200I16
L04200I16 V08000
DSTOP
```

??:

```text
L0    = OSR/SR6 linear stroke axis
4200  = position 0.42? 0-9999? ??
I16   = 16ms interval
V08000 = vibration axis V0? 0.8? ??
DSTOP = TCode device stop command
```

?? T-Code channel? ?? line?? ???? ?????.

????:

```text
HAPTIC_TCODE_LINEAR_AXIS=L0
HAPTIC_TCODE_VIBRATION_AXIS=V0
HAPTIC_TCODE_INTERVAL_MS=16
```

`HAPTIC_TCODE_VIBRATION_AXIS`? ??????. ??? ??? ?? ???.

## 12. ?? ???

??? ???:

- WebSocket only
- Socket.IO long polling upgrade ????
- compression ????
- motion event ack ??
- `volatile` event ??
- ?? frame ?? coalescing
- 4??? binary packet
- token bucket rate limit
- SerialPort backpressure ??

?? ??:

```text
??? ?????.
??? frame? ?? ?? ??? ??? ?? frame? ?? ? ??.
```

??? ?? viewer?? ??? frame backlog? ?? ????.

## 13. Rate Limit

?? rate limit? token bucket ?????.

????:

```text
HAPTIC_RELAY_MAX_HZ=60
HAPTIC_RELAY_BURST_FRAMES=2
```

?? millisecond interval ??? 60Hz ???? `Date.now()` ??? ??? ??? drop? ??????. token bucket?? ?? ? 500?/60Hz ????? ???? ?? ??????.

## 14. ?? ??? ??

?? ??? ??:

```text
50? / 30Hz / 5?     = 100% ??
500? / 30Hz / 10?   = 100% ??
500? / 60Hz / 10?   = 99.33% ??
1000? / 30Hz / 10?  = 100% ??
```

?? registry/Control API ?? ? ??:

```text
500? / 30Hz / 10? = 100% ??
```

??:

? ??? ?? ?? ??? ?????. ????? TLS, ?? ??? RTT, ???? CPU, NIC, ?? ??, ??? ???? ??? ?? ???? ???.

## 15. ?? ??? ??

?? ??:

```bash
npm run server:dev
```

500?, 30Hz:

```powershell
$env:VIEWERS=500
$env:HZ=30
$env:DURATION_MS=30000
npm.cmd run load:relay
```

500?, 60Hz:

```powershell
$env:VIEWERS=500
$env:HZ=60
$env:DURATION_MS=30000
npm.cmd run load:relay
```

?? ?:

```json
{
  "viewers": 500,
  "hz": 30,
  "sentFrames": 300,
  "expectedViewerFrames": 150000,
  "receivedViewerFrames": 150000,
  "receiveRate": 1
}
```

## 16. ?? ???

???:

```text
HAPTIC_RELAY_MAX_HZ=30
HAPTIC_RELAY_CLIENT_MAX_HZ=30
HAPTIC_HARDWARE_MAX_HZ=60
HAPTIC_MAX_VIEWERS_PER_ROOM=500
```

??? ??:

```text
HAPTIC_RELAY_MAX_HZ=60
HAPTIC_RELAY_CLIENT_MAX_HZ=60
HAPTIC_MAX_VIEWERS_PER_ROOM=500
```

?? ?? ??:

```text
???: 500? x 30Hz
???: 500? x 60Hz
???: 1000? x 30Hz
???: 1000? x 60Hz ??
```

1000? x 60Hz ??? ?? relay node?? ? ?? ?? ?? ?? relay node ??? ????.

## 17. ??? ??

```text
server/src/relay-server.ts
  HTTP Control API? Socket.IO relay server.

server/src/room-registry.ts
  RoomRegistry interface, InMemoryRoomRegistry, RedisRoomRegistry, RelayDirectory.

server/src/control-token.ts
  HMAC signed relay token.

src/shared/motion-packet.ts
  ??? binary packet encoder/decoder.

electron/motion-packet.ts
  Electron main process? binary packet encoder.

electron/services/relay-client.ts
  Control API ?? ? ??? relayUrl? Socket.IO ??, viewer motion packet ??, ???? ?? ???, ??? ?? ??? ??.

electron/services/hardware-controller.ts
  SerialPort ??, T-Code capability probe, latest frame queue, write drain ??.

electron/services/tcode-encoder.ts
  MotionFrame -> T-Code ??, probe command ??, probe ?? ??.

scripts/relay-load-test.mjs
  relay fanout ?? ???.
```

## 18. ???? ?? ????

```text
5b67d98 feat: scaffold haptic relay desktop app
0e02b70 feat: split relay server from desktop app
41e1065 perf: coalesce motion relay frames
f55fda5 fix: emit t-code for serial hardware
50bf3bc perf: encode relay motion as binary packets
47b0d5e perf: add relay load test and token bucket
1ad1d68 feat: add control api relay tokens
b82b897 feat: add relay node room assignment
7327fe2 feat: add redis room registry
74d1788 docs: add implementation guide
dd0a0f7 feat: route viewer motion to hardware
```

## 19. ?? ?? ??

1. adaptive Hz ?? ??
2. Redis live integration test
3. Dockerfile / production deployment
4. TLS termination / reverse proxy ??
5. Prometheus metrics ?? structured logging
6. Control API? ?? ???? ??
7. Postgres user/account/billing/moderation schema
8. ?? ??/?? ?? ???

## 20. ??? ?? ??

```text
1. Motion hot path? ?? ????? ??.
2. Redis/Postgres? control plane?? ??.
3. ?? ??? relay node? ????.
4. ?? viewer ??? ?? ? ??? ???? ? ??.
5. ??? motion frame? ??? ??.
6. ???? ??? T-Code? ??? ????.
7. ??? ??? ??? ? ?? relay ??? ?? ????.
```

?? ?? ??? ?????.
